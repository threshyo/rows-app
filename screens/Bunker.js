import { Linking } from "react-native";
import React from "react";
import {
  View,
  SafeAreaView,
  TouchableOpacity,
  Text,
  StyleSheet,
} from "react-native";
import Colors from "../constants/colors";
import { Entypo } from "@expo/vector-icons";
import { RowItem, RowSeparator, StoryText } from "../components/RowItem";

export default () => {
  return (
    <SafeAreaView>
You get everyone into the bunker but then Kanye West puts out a show telling everyone to not listen to it and a protest begins, causing the meteor to kill everyone. You never saw your wife.
    </SafeAreaView>
  );
};